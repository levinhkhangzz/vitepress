import { MatchResult } from 'path-to-regexp';
import { D as Dictionary, i as LunariaConfig, a as LocalizationStatus, k as Locale } from '../shared/core.8aeb6b08.mjs';
import { z } from 'zod';

declare const DictionaryContentSchema: z.ZodType<Dictionary>;

declare function getLocalizationStatus(config: LunariaConfig, isShallowRepo: boolean): Promise<LocalizationStatus[]>;
declare function getPathResolver(pattern: string, defaultLocale: Locale, locales: Locale[]): {
    isMatch: (path: string) => MatchResult<{
        lang?: string | undefined;
        path: string;
    }>;
    toLocalePath: (path: string, lang: string) => string;
    toSharedPath: (path: string) => string;
};

export { DictionaryContentSchema, getLocalizationStatus, getPathResolver };
