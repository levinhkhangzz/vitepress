import * as simple_git from 'simple-git';
import { i as LunariaConfig } from '../shared/core.8aeb6b08.mjs';
import 'zod';

declare const git: simple_git.SimpleGit;
/** Creates a clone of the git history to be used on platforms
 * that only allow shallow repositories (e.g. Vercel) and returns
 * `true` if it's running on a shallow repository.
 */
declare function handleShallowRepo({ cloneDir, repository }: LunariaConfig): Promise<boolean>;
declare function getFileHistory(path: string): Promise<{
    latest: (simple_git.DefaultLogFields & simple_git.ListLogLine) | null;
    all: readonly (simple_git.DefaultLogFields & simple_git.ListLogLine)[];
}>;
declare function getGitHostingLinks(repository: LunariaConfig['repository']): {
    create: (filePath: string) => string;
    source: (filePath: string) => string;
    history: (filePath: string, sinceDate?: string) => string;
    clone: () => string;
};

export { getFileHistory, getGitHostingLinks, git, handleShallowRepo };
